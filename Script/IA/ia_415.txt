{'user': 'Anonymous', 'message': '1 / 1atue como programador javascript escrevendo uma extensão para o chrome e me ajude com minhas dúvidas: preciso saber em qual url atualmente estou no javascript, como faço? Please write in Portuguese language.1 / 1Para obter a URL atual em JavaScript, você pode usar a propriedade "location" do objeto "window". Você pode acessar a URL atual usando "window.location.href". Por exemplo:javascriptCopy codelet urlAtual = window.location.href; console.log(urlAtual); Isso exibirá a URL atual no console do navegador. Se você quiser apenas o nome de domínio, você pode usar "window.location.hostname" em vez de "window.location.href".1 / 1adapte o código para identificar se a url atual é do google ou mercadolivre Please write in Portuguese language.1 / 1Para verificar se a URL atual é do Google ou do Mercado Livre, você pode usar o método "includes()" da String em JavaScript. Por exemplo:javascriptCopy codelet urlAtual = window.location.href; if (urlAtual.includes("google")) {  console.log("Esta é uma URL do Google."); } else if (urlAtual.includes("mercadolivre")) {  console.log("Esta é uma URL do Mercado Livre."); } else {  console.log("Esta é uma URL desconhecida."); } Este código verifica se a URL atual contém a string "google" ou "mercadolivre" e exibe a mensagem apropriada no console. Note que o método "includes()" é sensível a maiúsculas e minúsculas, então ele só vai funcionar corretamente se a string de pesquisa for escrita corretamente.1 / 1agora escreva uma função que irá escrever um iframe para buscar uma página específica após os resultados da busca do google Please write in Portuguese language.1 / 1Para criar um iframe que busca uma página específica após os resultados da pesquisa do Google, você pode usar a API de busca do Google para obter os resultados e, em seguida, criar um iframe com a URL da página específica. Aqui está uma possível implementação em JavaScript:javascriptCopy codefunction buscarPaginaEspecifica(query, urlEspecifica) {  // Constrói a URL da pesquisa do Google com a consulta fornecida  let urlBuscaGoogle = "https://www.google.com/search?q=" + encodeURIComponent(query);  // Faz uma requisição para a API de busca do Google para obter os resultados  fetch(urlBuscaGoogle)  .then(response => response.text())  .then(html => {  // Extrai o primeiro resultado da pesquisa do Google  let parser = new DOMParser();  let doc = parser.parseFromString(html, "text/html");  let resultado = doc.querySelector("div.g > div > div.rc > div.r > a");  // Verifica se um resultado foi encontrado  if (resultado) {  // Constrói a URL da página específica e cria o iframe  let urlPaginaEspecifica = resultado.href + urlEspecifica;  let iframe = document.createElement("iframe");  iframe.src = urlPaginaEspecifica;  document.body.appendChild(iframe);  } else {  console.error("Nenhum resultado encontrado.");  }  })  .catch(error => {  console.error(error);  }); } Esta função recebe dois parâmetros: a consulta a ser pesquis