Ejemplo de rutas en Power BI

En Power BI, una ruta se refiere a la secuencia de pasos que un usuario sigue para llegar a un informe o visualización específica. Un ejemplo de ruta en Power BI podría ser el siguiente: Esta es solo una posible ruta en Power BI, ya que el proceso exacto dependerá de los datos y requisitos específicos de cada proyecto.

Como crear una vista en sql server que desglose un campo de coordenadas de un ruta , es decir un array de parejas latitud longitud de la forma [[lat,lon]....[lat,lon]]

Para crear una vista en SQL Server que desglose un campo de coordenadas de un ruta que es un array de parejas latitud longitud, puedes utilizar la función  OPENJSON  para convertir el JSON de la ruta en filas y columnas de la tabla. Aquí hay un ejemplo de cómo puedes hacerlo: Supongamos que tienes una tabla llamada  Rutas  con una columna  Coordenadas  que contiene la ruta en formato JSON, que luce así: Puedes crear una vista que desglose las coordenadas en columnas separadas de  Latitud  y  Longitud  utilizando la siguiente consulta: En la consulta, utilizamos la función  OPENJSON  para convertir el JSON en filas y columnas, y luego extraemos la  Latitud  y  Longitud  utilizando la función  JSON_VALUE . Con la vista creada, puedes consultar las coordenadas de la ruta en columnas separadas utilizando la siguiente consulta: Espero que esto te ayude a crear la vista que necesitas.

se puede hacer con sql server 2012?

Sí, es posible crear una vista en SQL Server 2012 que desglose un campo de coordenadas de un ruta que es un array de parejas latitud longitud, utilizando una técnica similar a la que mencioné anteriormente. En SQL Server 2012, en lugar de la función  OPENJSON , puedes usar la función  CROSS APPLY  y la función  PARSENAME  para desglosar los valores de latitud y longitud. Aquí está un ejemplo de cómo podrías hacerlo: Supongamos que tienes una tabla llamada  Rutas  con una columna  Coordenadas  que contiene la ruta en formato JSON, que luce así: Puedes crear una vista que desglose las coordenadas en columnas separadas de  Latitud  y  Longitud  utilizando la siguiente consulta: En la consulta, utilizamos la función  CROSS APPLY  y la función  STRING_SPLIT  para separar el JSON en filas y columnas, y luego extraemos la  Latitud  y  Longitud  utilizando la función  PARSENAME . Con la vista creada, puedes consultar las coordenadas de la ruta en columnas separadas utilizando la siguiente consulta: Espero que esto te ayude a crear la vista que necesitas en SQL Server 2012.