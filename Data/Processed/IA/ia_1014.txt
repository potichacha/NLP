write a react component for nextjs application that has a react-dropbox in the center of the page. When a user drops FCS files, upload the files to a new azure storage account container and parse the fcs files server sider, return the file names, and image of the FSCxSSC plot and the parsed FCS headers in a card for each file in a 12x12 grid

In order to create a Next.js application with the specified requirements, you'll need to follow these steps: First, create a new Next.js application using the following command: Next, navigate to the project folder and install the necessary dependencies: Now, let's create a React component with react-dropzone: Configure the Azure Storage SDK and create a function to upload the files: